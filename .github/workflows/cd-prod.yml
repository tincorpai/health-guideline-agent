name: CD Dev (Disabled until AWS infra is ready)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: CD Dev disabled placeholder
        run: echo "CD Dev is disabled until AWS infrastructure is ready."

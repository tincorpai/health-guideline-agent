name: CD Prod (Disabled until AWS infra is ready)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: CD Prod disabled placeholder
        run: echo "CD Prod is disabled until AWS infrastructure is ready."

